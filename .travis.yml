language: node_js

env:
  - DATABASE_URL=postgresql://coder:pg123@localhost:5432/reglist

node_js:
  - "12.18.3"

before_script:
  - psql -c 'create database  reglist_tests;' -U postgres
  - psql -U postgres -d reglist_tests -a -f users.sql

services:
  - postgresql